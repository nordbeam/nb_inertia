/**
 * The complete official Inertia 3.7 Vue 3 adapter surface.
 *
 * Keep this entrypoint deliberately thin. NbInertia's enhanced wrappers remain
 * available from their existing subpaths (for example `vue/useForm` and
 * `vue/router`), while this barrel gives consumers a stable package-level
 * import for every API exported by `@inertiajs/vue3`.
 */
export * from '@inertiajs/vue3';
