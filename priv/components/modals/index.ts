/**
 * Modal Components for nb_inertia
 *
 * This directory contains UI components for the modal system that are
 * copied to your codebase. You own these files and can customize them freely.
 *
 * The core modal logic (hooks, context, types) is provided by:
 *   @nordbeam/nb-inertia/react/modals
 *
 * These copied components provide the UI layer using shadcn/ui.
 */

export { ModalStackRenderer } from './ModalStackRenderer';
