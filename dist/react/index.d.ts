/**
 * The complete official Inertia 3.7 React adapter surface.
 *
 * Keep this entrypoint deliberately thin. NbInertia's enhanced wrappers remain
 * available from their existing subpaths (for example `react/useForm` and
 * `react/router`), while this barrel gives consumers a stable package-level
 * import for every API exported by `@inertiajs/react`.
 */
export * from '@inertiajs/react';
//# sourceMappingURL=index.d.ts.map